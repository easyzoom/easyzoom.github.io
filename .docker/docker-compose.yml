# 在仓库根目录执行：docker compose -f .docker/docker-compose.yml up --build
services:
  site:
    build:
      context: ..
      dockerfile: .docker/Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ..:/app
